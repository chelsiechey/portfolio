<h1>Courses</h1>
<%= link_to 'Add Course', new_education_course_path(@education) %>

<ul>
    <% @education.courses.each do |c| %>
        <li><%= link_to c.name, c %></li>
    <% end %>
</ul>
<%= link_to 'Back', education_path(@education) %>

